import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  nomeIngrediente: {
    borderColor: "#9e9e9e",
    borderWidth: 1,
    fontSize: 20,
    textAlign: "center",
    marginTop: 2,
  },
});

export default styles;
